apply plugin: 'com.android.library'
apply plugin: 'maven'

android {
    compileSdkVersion rootProject.ext.android.compileSdkVersion
    buildToolsVersion rootProject.ext.android.buildToolsVersion

    defaultConfig {
        minSdkVersion rootProject.ext.android.minSdkVersion
        targetSdkVersion rootProject.ext.android.targetSdkVersion
        versionCode 20
        versionName "3.2.3"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    lintOptions {
        abortOnError false
    }
}

allprojects {
    tasks.withType(Javadoc) {//兼容中文字符
        options{
            encoding "UTF-8"
            charSet 'UTF-8'
            links "http://docs.oracle.com/javase/7/docs/api"
        }
    }
}

dependencies {
    api fileTree(include: ['*.jar'], dir: 'libs')
}

//uploadArchives{
//    repositories.mavenDeployer{
//        //这里的url可以换成自己的仓库地址
//        repository(url:"http://39.108.120.100:8090/repository/AndroidLib/"){
//            authentication(userName:"army", password:"111111")
//        }
//        //api "com.juziwl.exue:pickerview:0.0.1"
//        pom.version = "0.0.2"
//        pom.artifactId = "pickerview"
//        pom.groupId = "com.juziwl.exue"
//    }
//}

uploadArchives{
    repositories.mavenDeployer{
        //这里的url可以换成自己的仓库地址
        repository(url:"http://192.168.129.198:8081/nexus/content/repositories/AndroidLib/"){
            authentication(userName:"admin", password:"admin123")
        }
        //api "com.juziwl.exue:pickerview:0.0.4"
        pom.version = "0.0.4"
        pom.artifactId = "pickerview"
        pom.groupId = "com.juziwl.exue"
    }
}